# Dumps historicos

Los archivos SQL de esta carpeta no son equivalentes entre si y no deben
concatenarse ni importarse automaticamente. Se conservaron con nombres que
indican su ubicacion anterior.

`backup_antes_reconstruccion_20260708.sql` permanece protegido en la raiz del
proyecto y no se movio durante la reorganizacion.
